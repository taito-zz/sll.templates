Log in as the portal owner::

    >>> from plone.app.testing import TEST_USER_NAME, TEST_USER_PASSWORD
    >>> browser.login(TEST_USER_NAME, TEST_USER_PASSWORD)

    >>> browser.open('/folder/@@development-work')

    >>> 'Event01' in browser.contents
    True
    >>> 'Event02' in browser.contents
    True

    >>> '48 hours 0 minutes' in browser.contents
    True
    >>> '1440.00 EUR' in browser.contents
    True
    >>> '331.20 EUR' in browser.contents
    True
    >>> '1771.20 EUR' in browser.contents
    True
